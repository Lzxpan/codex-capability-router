[project]
name = "codex-capability-router"
# 修改紀錄（2026-08-17，Steve Peng）：將 package release metadata 與 beta tag 統一。
# 原始內容：version = "0.1.0"。
# 修改原因：本次公開發布目標是 v0.1.0-beta.1，不應讓 package metadata 誤稱 stable。
# 修改後功能：pyproject 與 GitHub beta release 使用同一個 beta.1 版本識別。
# 修改紀錄（2026-08-21，Steve Peng）
# 原始內容：version = "0.1.0-beta.1"。
# 修改原因：beta.3 release preparation 必須讓 package metadata 與待發布版本一致。
# 修改後功能：pyproject 使用 v0.1.0-beta.3 版本識別；不改變 Router production behavior。
# 修改紀錄（2026-08-25，Steve Peng）
# 原始內容：version = "0.1.0-beta.3"。
# 修改原因：beta.4 release preparation 必須讓 package metadata 與待發布版本一致。
# 修改後功能：pyproject 使用 v0.1.0-beta.4 版本識別；不改變 Router production behavior。
# 修改紀錄（2026-08-26，Steve Peng）
# 原始內容：version = "0.1.0-beta.4"。
# 修改原因：v0.2.0-beta.1 release preparation 必須反映已完成的 TaskAnalysis 與 Supporting contract。
# 修改後功能：公開 package 版本改為 v0.2.0-beta.1；不新增第二套版本欄位。
# 修改紀錄（2026-08-26，Steve Peng）
# 原始內容：description 僅描述 context-aware capability recommendations。
# 修改原因：v0.2 public metadata 必須反映 TaskAnalysis、Skill routing 與 certified Supporting scope。
# 修改後功能：公開精簡且不承諾 universal Provider support 的 package description。
# 修改紀錄（2026-09-02，Steve Peng）
# 原始內容：version = "0.2.0-beta.1"。
# 修改原因：v0.2.0-beta.2 修正 discovery source semantics、Plugin declaration resolution 與 blind inventory integrity。
# 修改後功能：公開 v0.2.0-beta.2 development version；保留 beta.1 historical references。
# 修改紀錄（2026-09-02，Steve Peng）
# 原始內容：version = "0.2.0-beta.2"。
# 修改原因：beta.3 修正 live discovery schema、Plugin identity contract 與 existence-only consideration。
# 修改後功能：公開 v0.2.0-beta.3 development version；保留 beta.1/beta.2 historical references。
# 修改紀錄（2026-09-02，Steve Peng）
# 原始內容：version = "0.2.0-beta.3"。
# 修改原因：beta.4 將 unknown Host hierarchy 轉為可 consideration 的 host_tool fallback。
# 修改後功能：公開 v0.2.0-beta.4 development version；保留 beta.1/beta.2/beta.3 historical references。
# 修改紀錄（2026-09-03，Steve Peng）
# 原始內容：version = "0.2.0-beta.4"。
# 修改原因：beta.5 收斂 authoritative-path discovery 與 declared-capability retention。
# 修改後功能：公開 v0.2.0-beta.5 development version；保留 beta.1/beta.2/beta.3/beta.4 historical references。
# 修改紀錄（2026-09-03，Steve Peng）
# 原始內容：version = "0.2.0-beta.5"。
# 修改原因：beta.6 補上 logical Plugin 到官方 PluginStore root 的 bounded resolution。
# 修改後功能：公開 v0.2.0-beta.6 development version；保留 beta.1-beta.5 historical references。
# 修改紀錄（2026-09-03，Steve Peng）
# 原始內容：version = "0.2.0-beta.7"。
# 修改原因：beta.8 清理 Skill semantic selection 的保守 redundancy/materiality wording。
# 修改後功能：公開 v0.2.0-beta.8 development version；不改變 runtime dependency 邊界。
# 修改紀錄（2026-09-04，Codex）
# 修改原因：beta.9 修正 canonical Skill 多 physical source 的 handoff freshness 綁定。
# 修改後功能：公開 v0.2.0-beta.9 development version；不改變 runtime dependency 邊界。
version = "0.2.0-beta.9"
description = "A read-only Codex Router for LLM TaskAnalysis, Skill routing, and certified Supporting capabilities."
requires-python = ">=3.11"
dependencies = []
