# Codex Capability Router v0.1.0-beta.1 Phase 5R / Phase 5C Validation Report

日期：2026-08-17
範圍：Phase 5R release-readiness remediation 與 Phase 5C release gate amendment；不開始 Phase 6。

## 1. Plugin Eval 結果與 budget 分類

- Fresh `explain-budget` deferred estimate：43,789 tokens。
- Total estimated budget：44,200 tokens。
- Plugin Eval budget type：`estimated-static`。
- 此數值是 static estimate，不是 measured runtime usage。

## 2. 原始 findings

### BLOCKER

- 缺少單一 canonical 12-scenario validation fixture、bounded validation test 與 Phase 5 validation artifact。
- Runtime capability envelope、source precedence、provenance/conflict model 尚未落地。

### IMPORTANT

- `unknown` capability 仍可能進入正常 recommendation/optional routing。
- Approved CLI probe failure handling 與 partial/unknown evidence 尚未落地。

### Budget finding

- Fresh `explain-budget` 仍列出 deferred cost 43,789 tokens；最大單一 contributor 是 repository documentation artifact，約 13,590 tokens。
- Deferred corpus 包含 repository artifacts；Plugin Eval 沒有提供 runtime reachability、actual deferred loading behavior 或 measured runtime token consumption。
- 因此 static deferred estimate 保留為 diagnostic information，不視為 beta functional release blocker。

## 3. 本次修復

- `tests/fixtures/routing_scenarios.json` 成為既有 12 個 scenario 的唯一 source of truth；6 個 `zh-TW`、6 個 `en`。
- `tests/test_routing_phase3.py` 改讀同一 JSON，不再維護 Python `SCENARIOS` 副本。
- 新增 `tests/test_bounded_validation.py`，驗證數量、語言、primary/optional limits、self-routing、unavailable/unknown 排除與 determinism。
- `CapabilityRecord` 增加 provenance、confidence、conflicts、evidence、recommendation-only 欄位；registry merge 明確採 runtime > CLI > explicit skill root > manual。
- 新增核准的 `codex plugin list --json`、`codex mcp list --json` bounded probe；non-zero、不可執行與 malformed schema 回 `partial + warning + unknown`，不 crash、不猜測。
- `unknown` 不再進 selected 或 normal recommendation；只有 trusted runtime/manual 明確標記 `recommendation_only=true` 才能進 advisory-only output。
- `SKILL.md` 縮為 operational contract，詳細 discovery/routing/i18n 移至 `references/`。

## 4. 不支援與跳過項目

- Benchmark dry-run：目前 CLI 不支援；本報告只保留 static diagnostic，不視為測試失敗。
- Optional external review/management tooling 不屬於本次 deterministic gate，未納入功能判定。
- 本文件是 Phase 5C 的歷史證據；remote release、tag 與 push 狀態以 Git history 與 GitHub release record 為準。

## 5. Deterministic verification

執行命令與結果：

```text
python -m unittest discover -s tests -v
Ran 36 tests in 0.038s
OK

python -m compileall -q codex_capability_router tests
exit code 0

python -c "import json; from pathlib import Path; p=Path('tests/fixtures/routing_scenarios.json'); x=json.loads(p.read_text(encoding='utf-8')); assert len(x)==12; assert sum(i['language']=='zh-TW' for i in x)==6; assert sum(i['language']=='en' for i in x)==6"
exit code 0

python -c "from pathlib import Path; files=[p for p in Path('.').rglob('*') if p.is_file() and '.git' not in p.parts and '__pycache__' not in p.parts]; assert not any('\ufffd' in p.read_text(encoding='utf-8') for p in files)"
exit code 0

git diff --check
exit code 0
```

Approved runtime probe evidence：deterministic tests 覆蓋兩個核准命令的 failure adapter；失敗保留兩筆 `unknown`、`partial=True` 與 `probe_failed` evidence，沒有 crash 或 available guess。

## 6. Remaining limitations

- CLI probe availability 仍不代表目前 plugin/MCP inventory 已驗證可用；失敗時維持 `unknown` 與 `partial`。
- 本次只驗證 software/static/deterministic behavior；未驗證外部 capability execution、hardware、physical path 或其他 real-world acceptance。
- Plugin Eval numeric score 不是 beta functional release gate；static deferred-token finding 保留為 known limitation，不進行第二輪 optimization。

## 7. Recorded static budget evidence

本報告只保留既有 Plugin Eval static analysis 的公開摘要；本 release 階段不重新執行 benchmark 或 analyze，也不把 evaluator score 當成 runtime usage：

- Deferred context estimate：43,789 tokens。
- Total estimated budget：44,200 tokens。
- Budget type：`estimated-static`。
- Repository documentation、tests、fixtures 與 implementation artifacts 都可能被納入 static estimate。

本次已完成一次 focused progressive-disclosure cleanup；measured runtime token usage 仍不可得，依規格不進行第二輪 optimization。

## 8. Phase 5C Release Gate Amendment

### Beta readiness

Release readiness: READY FOR BETA
Beta Release Gate: READY

### Stable readiness

Stable readiness: NOT READY
Stable Release Gate: NOT READY FOR STABLE

Reason:

Measured runtime token usage is unavailable.

### Static budget status

- Static estimated deferred cost: 43,789 tokens。
- Plugin Eval budget type: `estimated-static`。
- Total estimated budget: 44,200 tokens。
- Static deferred-cost estimate is retained as diagnostic information and is not treated as a functional beta release blocker。

### Known limitation

Runtime token budget has not been empirically measured.

Plugin Eval static deferred-cost analysis includes repository artifacts that are not proven to be loaded during normal skill invocation.

Therefore the static deferred-cost value is retained as diagnostic information and is not treated as measured runtime consumption。

### Deferred Stable Release Requirement

正式 v0.1.0 Stable Release 前，至少需要其中一項：

A. A supported Plugin Eval/runtime measurement mechanism becomes available and measured usage is acceptable。

B. Independent evidence proves the actual runtime loading model and establishes an acceptable bounded token budget。

Phase 5C 不實作 A 或 B，只將其記錄為 Stable Release requirement。

### Release classification

目前版本標記為 `v0.1.0-beta.1`，不得標記為 `v0.1.0 stable`。

Beta scope：

- feature-complete for v0.1 scope
- deterministic tests pass
- security/routing safeguards implemented
- public testing allowed
- runtime token cost not yet empirically measured
- API/schema may still receive compatible corrections before stable v0.1.0

其餘 deterministic/software functional gate 已通過；不得把 optional warnings 或 unsupported dry-run 當成 blocking reason。
